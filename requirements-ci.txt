# Lightweight CI requirements — excludes heavy GPU deps (jupyter, streamlit)
# Used by .github/workflows/ci.yml to keep runner install under 2 min

# Core Data Science
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Machine Learning
scikit-learn>=1.3.0
imbalanced-learn>=0.11.0
xgboost>=2.0.0

# Deep Learning (CPU-only — pinned to last release with standalone py3.11 wheel)
# tensorflow-cpu was merged into the single `tensorflow` package from 2.16 onwards.
# 2.15.0 is the last version with a confirmed linux-x86_64 / Python 3.11 wheel on PyPI.
tensorflow-cpu==2.15.0

# Explainability
shap>=0.44.0
lime>=0.2.0

# Visualization (headless-safe)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0

# Utilities
joblib>=1.3.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# Testing & Linting
pytest>=7.4.0
pytest-cov>=4.1.0
flake8>=6.1.0
